spring:
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name:
    username: postgres
    password: POSTGRES
    url: jdbc:postgresql://localhost:5432/easyquiz

